import React from "react";
import {Container} from "react-bootstrap";

const Home = () => {
    return (
        <Container fluid style={{padding: '0'}}>
            Home
        </Container>
    );
};

export default Home;